//console.log('messages here');
function send() {
    var message     = $('#message').val(),
        location    = window.location,
        protocol    = location.protocol,
        host        = location.host;
}